# Project Docs

Project-level state docs live here so the repository root stays small.

- `PROJECT_STATE.md`: high-level state index.
- `PROJECT_STATE_HUNTER_SEEKER.md`: Hunter-Seeker/Ouro state index.

Evaluator/domain-transfer notes live in `../evaluator/`.

The two master navigation docs remain at the repository root:

- `../../PROJECT_TREE_MAP.md`
- `../../PROJECT_COMPONENTS.md`
