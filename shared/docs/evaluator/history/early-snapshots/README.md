# Early Evaluator Snapshots

Chronological snapshots and handoffs from the earlier evaluator and branch-generation work. These are preserved for provenance; the current reading path starts in `../README.md` and the root evaluator docs.

