# Requirements

Environment dependency manifests live here.

- `requirements.txt`: main install requirements.
- `requirements.lock`: pinned lock-style snapshot.
- `requirements-pop-freeze.txt`: historical pip-freeze snapshot.

