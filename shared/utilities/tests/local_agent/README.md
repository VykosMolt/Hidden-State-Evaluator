# Local-Agent Tests

- `test_local_agent_wrapper.py`: project-side wrapper unit coverage.
- `test_conversation_routing.py`: local-agent conversation/routing checks.
- `test_doctor_report.py`: local diagnostic/report checks.

`pytest.ini` includes this directory in the project test paths.
