# Compatibility

Compatibility notes for old paths live here instead of cluttering the project
root.

- `legacy_paths/`: current canonical replacements for old checkpoint,
  trajectory, and environment cache locations.

New code should use the canonical `data/` and `artifacts/` paths directly.
